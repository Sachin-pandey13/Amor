[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "amor-ai"
version = "0.1.0"
description = "AMOR - Adaptive, personalized and privacy-first AI agent"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]